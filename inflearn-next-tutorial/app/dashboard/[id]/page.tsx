export default function DashBoardDetailPage({params, searchParams}) {
    console.log('params',params);
    console.log('searchParams',searchParams);
    return (
        <main>DashboardDetail</main>
    );
}